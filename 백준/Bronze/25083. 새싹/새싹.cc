#include <iostream>
#include <cstring>
#include <string>

using std::cin;
using std::cout;
using std::string;

//#define SWAP(x,y,t) ((t) = (x), (x) = (y), (y) = (t))

int main(void) {
	cin.tie(NULL);
	std::ios_base::sync_with_stdio(false);

	cout << "         ,r\'\"7\n";
	cout << "r`-_   ,\'  ,/\n";
	cout << " \\. \". L_r\'\n";
	cout << "   `~\\/\n";
	cout << "      |\n";
	cout << "      |\n";

	return 0;
}
/*
		 ,r'"7
r`-_   ,'  ,/
 \. ". L_r'
   `~\/
	  |
	  |

*/